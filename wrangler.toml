name = "lunch-and-learn"
compatibility_date = "2025-07-15"
pages_build_output_dir = "dist"

# ---------------------------------------------------------------------------
# Env vars & secrets are NOT declared here.
#
# Because this file exists, Cloudflare manages plaintext vars from wrangler
# config and only allows Secrets in the dashboard. Add any runtime values
# (e.g. for feature branches that need API tokens) as Secrets in the Pages
# dashboard: Settings → Variables and Secrets.
# ---------------------------------------------------------------------------
